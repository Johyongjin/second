# Second Project

Streamlit 기반 VAR 대시보드가 포함되어 있습니다. 아래 명령으로 실행하세요.

```bash
streamlit run streamlit_app.py
```

- 기본 데이터셋(df_var_1209.csv)을 사용하거나 사이드바에서 CSV를 업로드할 수 있습니다.
- VAR에 사용할 컬럼은 사이드바에서 선택 가능합니다(데이터에 없는 컬럼은 자동 제외).
